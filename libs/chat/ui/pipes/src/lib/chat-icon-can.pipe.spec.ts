import { ChatIconCanPipe } from './chat-icon-can.pipe';

describe('ChatIconCanPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatIconCanPipe();
    expect(pipe).toBeTruthy();
  });
});

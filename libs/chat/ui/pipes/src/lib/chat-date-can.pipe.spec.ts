import { ChatDateCanPipe } from './chat-date-can.pipe';

describe('ChatDateCanPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatDateCanPipe();
    expect(pipe).toBeTruthy();
  });
});

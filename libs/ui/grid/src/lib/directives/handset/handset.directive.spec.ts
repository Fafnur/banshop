import { HandsetDirective } from './handset.directive';

describe('HandsetDirective', () => {
  it('should create an instance', () => {
    const directive = new HandsetDirective();
    expect(directive).toBeTruthy();
  });
});

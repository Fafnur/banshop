import { CartProductPricePipe } from './cart-product-price.pipe';

/**
 * TODO: Add tests
 */
describe('CartProductPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartProductPricePipe();
    expect(pipe).toBeTruthy();
  });
});
